# Деплой бота в облако

Этот документ описывает различные варианты хостинга Telegram бота.

## 🚀 Вариант 1: Railway (Рекомендуется)

**Преимущества:**
- ✅ Очень простой деплой (1-2 клика)
- ✅ Бесплатный tier: $5 кредитов в месяц
- ✅ Автоматический деплой из GitHub
- ✅ Встроенная поддержка переменных окружения
- ✅ Автоматические перезапуски при падении

**Инструкция:**

1. **Подготовка репозитория:**
   - Убедитесь, что все файлы закоммичены и запушены в GitHub

2. **Регистрация на Railway:**
   - Перейдите на [railway.app](https://railway.app)
   - Войдите через GitHub

3. **Создание проекта:**
   - Нажмите "New Project"
   - Выберите "Deploy from GitHub repo"
   - Выберите ваш репозиторий `parking-bot`

4. **Настройка переменных окружения:**
   - В настройках проекта (Settings → Variables) добавьте все переменные из `.env`:
     ```
     TELEGRAM_BOT_TOKEN=ваш_токен
     GIGACHAT_CLIENT_ID=ваш_client_id
     GIGACHAT_CLIENT_SECRET=ваш_client_secret
     OWNER_USER_ID=ваш_user_id
     ALLOWED_CHAT_IDS=список_чатов
     RAFFLE_TIMER_SECONDS=120
     MAX_ACTIVE_RAFFLES=5
     ```

5. **Настройка деплоя:**
   - Railway автоматически определит Python проект
   - Если не определит, в настройках (Settings → Build) укажите:
     - **Build Command:** `pip install -r requirements.txt`
     - **Start Command:** `python src/bot.py`

6. **Деплой:**
   - Railway автоматически задеплоит проект
   - Проверьте логи (Deployments → View Logs)

**Стоимость:** Бесплатно до $5/месяц, затем $5/месяц за базовый план

---

## 🌐 Вариант 2: Render

**Преимущества:**
- ✅ Бесплатный tier (с ограничениями)
- ✅ Простой деплой
- ✅ Автоматический деплой из GitHub

**Недостатки:**
- ⚠️ Бесплатные сервисы "засыпают" после 15 минут бездействия
- ⚠️ Первый запрос после пробуждения может быть медленным

**Инструкция:**

1. **Регистрация:**
   - Перейдите на [render.com](https://render.com)
   - Войдите через GitHub

2. **Создание Web Service:**
   - Нажмите "New +" → "Web Service"
   - Подключите ваш GitHub репозиторий

3. **Настройка:**
   - **Name:** `parking-bot`
   - **Environment:** `Python 3`
   - **Build Command:** `pip install -r requirements.txt`
   - **Start Command:** `python src/bot.py`

4. **Переменные окружения:**
   - В разделе "Environment" добавьте все переменные из `.env`

5. **Деплой:**
   - Нажмите "Create Web Service"
   - Render автоматически задеплоит проект

**Стоимость:** Бесплатно (с ограничениями), $7/месяц за платный план

---

## 🐳 Вариант 3: Fly.io

**Преимущества:**
- ✅ Хороший бесплатный tier
- ✅ Поддержка Docker
- ✅ Глобальная сеть

**Инструкция:**

1. **Установка CLI:**
   ```bash
   # Windows (PowerShell)
   powershell -Command "iwr https://fly.io/install.ps1 -useb | iex"
   ```

2. **Регистрация:**
   ```bash
   fly auth signup
   ```

3. **Создание приложения:**
   ```bash
   fly launch
   ```
   - Выберите регион (например, `ams` для Амстердама)
   - Не создавайте PostgreSQL (не нужен)

4. **Настройка переменных окружения:**
   ```bash
   fly secrets set TELEGRAM_BOT_TOKEN=ваш_токен
   fly secrets set GIGACHAT_CLIENT_ID=ваш_client_id
   fly secrets set GIGACHAT_CLIENT_SECRET=ваш_client_secret
   fly secrets set OWNER_USER_ID=ваш_user_id
   fly secrets set ALLOWED_CHAT_IDS=список_чатов
   fly secrets set RAFFLE_TIMER_SECONDS=120
   fly secrets set MAX_ACTIVE_RAFFLES=5
   ```

5. **Деплой:**
   ```bash
   fly deploy
   ```

**Стоимость:** Бесплатно до 3 shared-cpu-1x VM, затем платно

---

## 🖥️ Вариант 4: VPS (Hetzner, DigitalOcean)

**Преимущества:**
- ✅ Полный контроль
- ✅ Нет ограничений на "сон"
- ✅ Дешево (от €4/месяц)

**Недостатки:**
- ⚠️ Нужно управлять сервером самостоятельно
- ⚠️ Нужно настроить автозапуск и мониторинг

**Инструкция:**

1. **Создание VPS:**
   - Зарегистрируйтесь на [Hetzner](https://www.hetzner.com) или [DigitalOcean](https://www.digitalocean.com)
   - Создайте VPS (Ubuntu 22.04, минимальная конфигурация)

2. **Подключение к серверу:**
   ```bash
   ssh root@ваш_ip
   ```

3. **Установка зависимостей:**
   ```bash
   # Обновление системы
   apt update && apt upgrade -y
   
   # Установка Python и uv
   curl -LsSf https://astral.sh/uv/install.sh | sh
   source $HOME/.cargo/env
   
   # Клонирование репозитория
   git clone https://github.com/ваш_username/parking-bot.git
   cd parking-bot
   ```

4. **Настройка переменных окружения:**
   ```bash
   cp .env.example .env
   nano .env  # Заполните все переменные
   ```

5. **Установка зависимостей:**
   ```bash
   uv sync
   ```

6. **Настройка systemd для автозапуска:**
   ```bash
   sudo nano /etc/systemd/system/parking-bot.service
   ```
   
   Содержимое файла:
   ```ini
   [Unit]
   Description=Parking Bot
   After=network.target
   
   [Service]
   Type=simple
   User=root
   WorkingDirectory=/root/parking-bot
   Environment="PATH=/root/.cargo/bin:/usr/local/bin:/usr/bin:/bin"
   ExecStart=/root/.cargo/bin/uv run python src/bot.py
   Restart=always
   RestartSec=10
   
   [Install]
   WantedBy=multi-user.target
   ```

7. **Запуск сервиса:**
   ```bash
   sudo systemctl daemon-reload
   sudo systemctl enable parking-bot
   sudo systemctl start parking-bot
   sudo systemctl status parking-bot
   ```

8. **Просмотр логов:**
   ```bash
   sudo journalctl -u parking-bot -f
   ```

**Стоимость:** От €4/месяц (Hetzner) или $6/месяц (DigitalOcean)

---

## 📊 Сравнение вариантов

| Платформа | Сложность | Стоимость | Надежность | Рекомендация |
|-----------|-----------|-----------|------------|--------------|
| **Railway** | ⭐ Легко | $0-5/мес | ⭐⭐⭐⭐⭐ | ✅ **Лучший выбор** |
| **Render** | ⭐ Легко | $0-7/мес | ⭐⭐⭐ | ⚠️ Может "засыпать" |
| **Fly.io** | ⭐⭐ Средне | $0+/мес | ⭐⭐⭐⭐ | ✅ Хороший вариант |
| **VPS** | ⭐⭐⭐ Сложно | €4+/мес | ⭐⭐⭐⭐⭐ | ✅ Для продвинутых |

---

## 🔧 Общие рекомендации

1. **Мониторинг:**
   - Настройте уведомления о падении бота (Railway/Render делают это автоматически)
   - Регулярно проверяйте логи

2. **Безопасность:**
   - Никогда не коммитьте `.env` файл в репозиторий
   - Используйте секреты платформы для переменных окружения

3. **Резервное копирование:**
   - Код уже в GitHub - это хорошо
   - Рассмотрите возможность сохранения логов

4. **Производительность:**
   - Бот очень легкий, любой из вариантов справится
   - Для начала достаточно бесплатного tier

---

## 🚨 Решение проблем

### Бот не запускается
- Проверьте логи на платформе
- Убедитесь, что все переменные окружения установлены
- Проверьте формат переменных (без пробелов, кавычек)

### Бот падает
- Проверьте логи на наличие ошибок
- Убедитесь, что токены корректны
- Проверьте доступность GigaChat API

### Бот не отвечает в группе
- Убедитесь, что бот добавлен в группу
- Проверьте, что `ALLOWED_CHAT_IDS` содержит ID группы
- Проверьте настройки приватности бота в BotFather

---

## 📝 Следующие шаги

После деплоя:
1. Протестируйте бота в группе
2. Проверьте работу всех функций
3. Настройте мониторинг (если нужно)
4. Документируйте URL деплоя (если используете webhook вместо polling)

